{
  "name": "image-render-service",
  "version": "1.0.0",
  "description": "Puppeteer image render service",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
